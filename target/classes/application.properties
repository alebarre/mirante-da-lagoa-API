spring.application.name=mirante-lagoa

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5433/mirante_lagoa
spring.datasource.username=mirante
spring.datasource.password=mirante123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# JWT
app.jwt.secret=${JWT_SECRET:CC64fgj5kM1fQP/CBBaw+sgOfADU+JQY39u+L3ceJMym3Yqaqp5Aj3bu1Gcchuy2kPjqxhG0Xarckam7GXekew==}
app.jwt.access-token-expiration-ms=900000
app.jwt.refresh-token-expiration-ms=604800000

# Mail (ajuste conforme seu provedor)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USER:}
spring.mail.password=${MAIL_PASS:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# CORS
app.cors.allowed-origins=http://localhost:4200

# Server
server.port=8080
